#include <windows.h>

// Common Controls v6 (modern ListView / TreeView / theming) + DPI awareness.
// app.manifest is merged into the linker-generated manifest via the project's
// <Manifest> item, so it is intentionally NOT embedded here (avoids a duplicate).

101 ICON "bacon.ico"
